import { Router } from 'express';

const router = Router();

// This file is no longer in use. 
// Student admission is now handled securely via POST /api/students by an administrator.

export default router;